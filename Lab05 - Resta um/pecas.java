public class pecas{
		int linha,
			coluna; 
		char estado; // ' '; 'P'; '-'

		pecas(char estado, int coluna, int linha){
			this.linha = linha;
			this.coluna = coluna;
			this.estado = estado;
		}
	}